# TODOs

## Tasks

- [x] 速度ではなく移動度 $\mu = \frac{v}{f}$ を残すようにする
- [x] smoke モードを削除する
  - GPU は常に 0,1,2 を使うようにし `--allow-device-zero` オプションを削除する
  - trials.csv は production でも出力するようにする
- [x] パラメータ m, f, βpE, ΔαE/p をコマンドラインから与えられるようにする
  - それぞれのパラメータについて、リスト型などで複数個の値が与えられるようにする
  - 1/3 みたいな分数でも数値を与えたい。どうするのが良いか？ → `a/b` 表記をパースして f64 にする
- [x] 各 combo ごとに output/ 以下にフォルダを作って、結果を保存するようにする
  - 各 combo のフォルダ名は、`m{m}_f{f}_beta{beta}_delta{delta}` のようにする
  - フォルダ名を決めるためのオプションは削除する
- [x] コマンドオプションの使い方を説明する README.md を作る
- [x] 粒子の角度の分布（特に細い部分において）を記録するようにする
  - 採用方針: GPU上で2Dヒストグラム (x × φ) を `--hist-stride` ステップごとにatomicAddで集計し、
    1周期に畳んで全trial・全ステップを集計する。データ量はステップ数によらず固定（x_bins × φ_bins）。
    - x座標ごとの角度分布が `angle_hist.csv` で得られる（細い部分は x-bin を細かくすれば解像できる）。
    - 全ステップ保存ではなくヒストグラム集計なので、データ量は莫大にならない。
    - y座標の分布も同方式で (x × y) を `y_hist.csv` に記録（atomicAdd1回追加のみで速度影響小）。
      壁位置 ±omega(x) で正規化すれば、y方向に平行（一様）に達しているか確認できる。

## 決定事項

- N = 1000 のままにする
- 複数comboを同時に処理することで、A100のSMを占有率を上げる（measured kneeは16）
